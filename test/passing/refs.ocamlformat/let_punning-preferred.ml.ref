let ( let* ) x f = f x

let ( and* ) a b = (a, b)

let x = 1

and y = 2

and z = 3

let p =
  let* x and* y and* z in
  (x, y, z)

let q =
  let%foo x and y and z in
  (x, y, z)

let r =
  let*
      (* 1 *)
      (* 3 *) x
  (* 2 *)
  (* 4 *)
  and* (* 5 *) y
    (* 6 *)
  in
  (x, y)

let s =
  let%foo
      (* 1 *)
      (* 3 *) x
  (* 2 *)
  (* 4 *)
  and (* 5 *) y
    (* 6 *)
  in
  (x, y)
