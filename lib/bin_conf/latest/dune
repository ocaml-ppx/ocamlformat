(rule
 (action
  (write-file
   Bin_conf.ml
   ; enable release renaming
   "include Bin_conf_multi_version_alpha1"
   ; disable release renaming
   )))

(library
 (name bin_conf)
 (public_name ocamlformat.bin_conf)
 (libraries
  ; enable release renaming
  ocamlformat-multi_version_alpha1.bin_conf
  ; disable release renaming
  ))
