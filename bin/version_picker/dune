(executable
 (name version_picker)
 (public_name ocamlformat)
 (package ocamlformat)
 (link_deps
  (package
   ; enable release renaming
   ocamlformat-dev
   ; disable release renaming
   ))
 (libraries ocamlformat-lib ocamlformat.bin_conf unix)
 (flags
  (:standard
   -open
   ; enable release renaming
   Ocamlformat_ocaml_common_dev
   -open
   Ocamlformat_parser_extended_dev
   -open
   Ocamlformat_stdlib_dev
   -open
   Ocamlformat_format_dev
   ; disable release renaming
   )))

(rule
 (with-stdout-to
  ocamlformat.1
  (run
   ; enable release renaming
   ocamlformat-dev
   ; disable release renaming
   --help=groff)))

(install
 (section man)
 (files ocamlformat.1)
 (package ocamlformat))