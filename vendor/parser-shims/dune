(rule
 (action
  (copy
   ocamlformat_parser_shims.ml
   ; enable release renaming
   ocamlformat_parser_shims_dev.ml
   ; disable release renaming
   )))

(rule
 (action
  (copy
   ocamlformat_parser_shims.mli
   ; enable release renaming
   ocamlformat_parser_shims_dev.mli
   ; disable release renaming
   )))

(library
 (name
  ; enable release renaming
  ocamlformat_parser_shims_dev
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-lib-dev.parser_shims
  ; disable release renaming
  )
 (flags
  (:standard -w -37 -w -38))
 (libraries compiler-libs.common))
