(ocamllex lexer)

(library
 (name
  ; enable release renaming
  ocamlformat_odoc_parser_devversion
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-lib-devversion.odoc_parser
  ; disable release renaming
  )
 (instrumentation
  (backend bisect_ppx))
 (flags
  (:standard -w -50))
 (libraries astring camlp-streams))
