(ocamllex lexer)

(library
 (name
  ; enable release renaming
  ocamlformat_odoc_parser_multi_version_alpha1
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-lib-multi_version_alpha1.odoc_parser
  ; disable release renaming
  )
 (instrumentation
  (backend bisect_ppx))
 (flags
  (:standard -w -50))
 (libraries astring camlp-streams))
