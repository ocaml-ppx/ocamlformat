(rule
 (copy# warning/%{read:warning-compat-file} warning.ml))

(rule
 (with-stdout-to
  warning-compat-file
  (run warning/gen/gen.exe)))

(library
 (name compat)
 (libraries base compiler-libs.common either))
