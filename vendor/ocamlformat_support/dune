(library
 (name
  ; enable release renaming
  ocamlformat_format_devversion
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-lib-devversion.format_
  ; disable release renaming
  )
 (flags
  (:standard
   -open
   ; enable release renaming
   Ocamlformat_parser_shims_devversion
   ; disable release renaming
   ))
 (libraries
  either
  ; enable release renaming
  ocamlformat_parser_shims_devversion
  ; disable release renaming
  ))
