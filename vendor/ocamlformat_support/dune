(library
 (name
  ; enable release renaming
  ocamlformat_format_dev
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-dev.format_
  ; disable release renaming
  )
 (flags
  (:standard
   -open
   ; enable release renaming
   Ocamlformat_parser_shims_dev
   ; disable release renaming
   ))
 (libraries
  either
  ; enable release renaming
  ocamlformat_parser_shims_dev
  ; disable release renaming
  ))
