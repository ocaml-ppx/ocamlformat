(rule
 (action
  (copy
   ocamlformat_stdlib.ml
   ; enable release renaming
   ocamlformat_stdlib_multi_version_alpha1.ml
   ; disable release renaming
   )))

(rule
 (action
  (copy
   ocamlformat_stdlib.mli
   ; enable release renaming
   ocamlformat_stdlib_multi_version_alpha1.mli
   ; disable release renaming
   )))

(library
 (name
  ; enable release renaming
  ocamlformat_stdlib_multi_version_alpha1
  ; disable release renaming
  )
 (public_name
  ; enable release renaming
  ocamlformat-lib-multi_version_alpha1.ocamlformat_stdlib
  ; disable release renaming
  )
 (flags
  (:standard
   -open
   ; enable release renaming
   Ocamlformat_ocaml_common_multi_version_alpha1
   ; enable release renaming
   ))
 (libraries
  base
  cmdliner
  ; enable release renaming
  ocamlformat_ocaml_common_multi_version_alpha1
  ; disable release renaming
  fpath
  stdio))
